# .env is the default environment, so you must ensure .env file is filled first, then dev env
# variables are filled as well in .env.dev.
# Those variables are loaded by src/environment/env.tsx
# To add a new environment, add for example a .env.staging and add the corresponding scripts in
# package.json, e.g. `"build:staging": "env-cmd .env.staging npm run build",`.

# Production environment variables
REACT_APP_ENV=prod
REACT_APP_API_PATH=https://jsonplaceholder.typicode.com

# CRA-specific configuration
GENERATE_SOURCEMAP=false
